<%- include('../partials/header') %>


<link rel="stylesheet" href="{{asset('/css/admin-side-column.css')}}">

<title>BW Blairwalf | Modificar categoría</title>
    
</head>
<body>
  <div class="container-fluid">
    <div class="row">

        <%- include('../partials/side-column') %>

        <div class="col-sm-8 col-md-9">
            <p class="centrar">MODIFICAR CATEGORÍA</p>

            @if(session('message'))
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    {{session('message')}}
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
            @endif
            @if(session('message-error'))
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                {{session('message-error')}}
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
            @endif

            <form action="/admin/modificar-categoria" method="post" class="form">
                @csrf
                <div class="form-group">
                    <label for="category_title">Título</label>
                <input type="text" name="category_title" id="category_title" class="form-control" data-name="título" value="{{$categoria->category_title}}">
                    <p class="required"></p>
                </div>
                <div class="flex"><button type="submit" class="btn send-button">Modificar categoría</button></div>
            </form>
        </div>

      </div>
    </div>
    
    
   
<%- include('../partials/footer') %>


<script src="/js/alerts.js"></script>

  </body>
  </html>